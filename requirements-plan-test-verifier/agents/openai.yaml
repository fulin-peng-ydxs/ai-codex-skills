interface:
  display_name: "需求与计划正式验证"
  short_description: "按需求验收标准和开发任务做前后端分层验证，并输出可复测正式报告"
  default_prompt: "围绕唯一需求文档与开发计划做正式验证：以需求文档中的验收标准和开发计划中的前后端开发任务为基线，先建立验收标准与开发任务双向映射，再按后端构建测试、后端单元测试、前端构建测试、页面与组件交互验证、接口契约与字段映射校验、关键流程烟雾验证分层执行，并输出可复测的正式验证报告。"
