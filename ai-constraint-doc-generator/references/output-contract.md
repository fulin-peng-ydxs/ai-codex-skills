# AI 约束文档输出契约

## 适用规则

新建、重构或系统性优化 `AGENTS.md`、`CLAUDE.md` 时，默认遵守本契约。用户明确指定其它结构，或仓库存在上游工具要求的机器读取契约时，可以偏离；偏离项和原因必须在交付说明中列出。

标题语言跟随文档语言。中文使用下述固定标题；用户明确要求英文时使用文末对应标题。除上述例外外，不修改固定章节名称和顺序，不新增其它 Markdown 二级章节。项目特有差异进入固定章节下的三级或更深标题。

## AGENTS.md 固定章节

一级标题使用项目名称或“项目 AI 协作说明”。以下二级章节全部必须保留、非空并按固定顺序出现：

```markdown
# {项目名称} AI 协作说明

## 文档职责、适用范围与规则优先级
## 项目事实与代码结构
## 变更边界与实现约束
## 命令与验证
## 项目特有契约
## 文档与产物同步
## 安全、确认门禁与禁止操作
## 完成标准
```

不存在额外项目特有契约时，保留对应章节并写明“当前未发现超出前述规则的额外项目契约”及判断依据；不要删除章节，也不要填充通用编程常识。

## AGENTS.md 各章最低内容

### 文档职责、适用范围与规则优先级

至少写明：

- 本文件约束的目录范围；
- 是否存在更深层 `AGENTS.md`，以及就近规则的覆盖关系；
- 用户明确要求、项目级规则和上级通用规则的优先关系；
- 代码、机器配置、README、架构、设计、需求等事实源发生冲突时如何处理。

### 项目事实与代码结构

至少写明项目用途、主要交付形态、关键源码/测试/配置/脚本/文档目录及职责。标出生成物、第三方代码、缓存、构建产物和默认禁止修改的区域。只写影响定位和变更的事实，不复述完整产品介绍。

### 变更边界与实现约束

至少规定：

- 修改前如何寻找同类实现和权威事实；
- 需要保持的分层、命名、API、数据、权限、错误处理或配置方式；
- 新增依赖、公共抽象、生成代码和跨模块改动的边界；
- 何种技术方案、交互或业务规则必须先确认。

规则必须来自当前项目，不复制与项目无关的通用开发口号。

### 命令与验证

按环境、依赖、测试、静态检查、构建、本地启动、状态或健康检查、停止清理的稳定顺序组织。推荐使用表格：

| 目的/触发条件 | 工作目录 | 已验证命令 | 成功判定 | 清理或恢复 |
|---|---|---|---|---|

所有作为日常流程写入的命令必须通过 `verification-gate.md`；任一落地必验命令失败时，不覆盖 `AGENTS.md` 或 `CLAUDE.md`。长期服务必须包含启动、就绪判断和停止闭环。

### 项目特有契约

按 `document-model.md` 的固定三级标题选择项目特有内容。至少覆盖会影响正确性、安全性、权限、数据一致性、兼容性、服务生命周期、构建产物或发布结果的现有契约。规则要写触发条件、必须动作或禁止动作，以及可复核的事实入口。

### 文档与产物同步

至少写明：

- 什么改动会触发 README、架构、设计、API、需求、部署或 AI 规则更新；
- 需求、计划、审查、SQL、测试记录等项目产物的稳定目录规则；
- 同一事实的权威位置和其它文档的引用方式；
- 完成改动后必须检查哪些事实文档是否需要同步。

没有专用产物目录时，说明遵循仓库已有布局，不自行创建零散根目录文档。

### 安全、确认门禁与禁止操作

至少覆盖与项目实际相关的凭证、生产或外部系统写入、数据库变更、破坏性文件/Git 操作、依赖安装、长任务和用户确认边界。明确哪些行为可自动执行、哪些必须先确认、哪些始终禁止。不得写入真实秘密。

### 完成标准

至少包含：

- 以第三方视角自审本次改动并修复发现的问题；
- 检查同类位置是否存在相同问题或受影响能力；
- 按风险执行已验证的最低充分测试、构建或运行检查；
- 检查文档和规范是否需要同步；
- 汇报变更文件、验证命令、未覆盖项和残余风险。

## CLAUDE.md 固定结构

`CLAUDE.md` 是薄入口，默认使用：

```markdown
# Claude Code 项目入口

## 权威规则入口
## 工作方式
## 记忆边界
```

其中 `权威规则入口`、`工作方式` 必须保留且非空；`记忆边界` 仅在项目确实存在 Claude 记忆路径或策略时写入。不得新增其它 Markdown 二级章节。

最低内容：

- `权威规则入口`：明确仓库协作规则以 `AGENTS.md` 为准，并链接必要事实源；
- `工作方式`：要求改动前阅读 `AGENTS.md` 和相关实现，将命令、测试、端口和项目契约指回 `AGENTS.md`；
- `记忆边界`：只写稳定记忆规则，并明确仓库当前代码和文档优先于外部记忆。

`CLAUDE.md` 不复制命令块、完整项目规则、技术栈和长篇项目说明。

## 更新已有文档

- 保留仍有效的项目特有规则和命令，将其迁移到固定信息归属；不能为了套骨架删除安全、业务、数据或发布约束。
- 旧标题与固定章节语义相同则统一为固定标题；项目特有标题降为对应章节下的三级标题。
- 合并重复规则，`AGENTS.md` 保留权威正文，`CLAUDE.md` 和其它入口仅链接。
- 删除历史流水、临时案例、本机环境快照、过期命令和没有事实依据的规则。
- 重构标题前后检查仓库内引用和锚点，必要时同步更新。

## 英文章节标题

英文文档使用以下对应标题：

| 中文 | 英文 |
|---|---|
| 文档职责、适用范围与规则优先级 | Document Scope and Rule Priority |
| 项目事实与代码结构 | Project Facts and Code Structure |
| 变更边界与实现约束 | Change Boundaries and Implementation Constraints |
| 命令与验证 | Commands and Verification |
| 项目特有契约 | Project-Specific Contracts |
| 文档与产物同步 | Documentation and Artifact Synchronization |
| 安全、确认门禁与禁止操作 | Safety, Confirmation Gates, and Prohibited Actions |
| 完成标准 | Definition of Done |
| 权威规则入口 | Authoritative Rule Entry |
| 工作方式 | Working Method |
| 记忆边界 | Memory Boundaries |

英文 `CLAUDE.md` 一级标题使用 `# Claude Code Project Entry`。

## 交付门禁

落地前必须确认：

- `AGENTS.md` 八个固定章节完整、非空、无重复且顺序正确；
- 项目特有内容位于固定章节下，没有额外 Markdown 二级章节；
- `CLAUDE.md` 保持薄入口且没有复制命令块；
- 命令验证门禁已经通过；
- 文档链接和受标题调整影响的仓库内锚点有效；
- 已运行 `scripts/validate_ai_constraint_docs.py <repo-root>` 并通过；存在已说明的用户或机器契约例外时使用 `--custom-structure`。
