interface:
  display_name: "会话级代码审查"
  short_description: "围绕当前会话代码做审查和最小充分验证"
  default_prompt: "围绕当前会话中改过的代码做会话级代码审查与最小充分验证，重点检查业务逻辑是否满足、代码复用和项目风格是否统一、是否存在编译/构建/类型错误、前后端接口联调是否有问题以及是否存在性能风险，并给出最小改动建议和验证结论。"
