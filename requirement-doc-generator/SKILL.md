---
name: requirement-doc-generator
description: 生成完整的项目化需求文档，用于在制定开发计划前澄清功能需求。适用于用户要求创建、补充、确认或保存需求文档、PRD、需求分析、requirement.md，或希望 AI 结合业务需求、当前项目代码、数据库实际情况、交互体验、边界条件、风险和验收标准来充分理解需求，并沉淀为可指导后续 AI 生成高质量开发计划的文档。
---

# 需求文档生成器

## 核心目标

将用户的初步功能想法整理成一份能指导后续 AI 制定高质量开发计划的 `requirement.md`。优先依据当前项目的真实代码、数据库、业务流程和已有交互模式，不要只输出通用产品描述。

## 执行流程

1. 确定功能名称和输出目录。
   - 输出目录必须相对目标项目根目录，而不是无关的空对话目录或技能管理目录。
   - 如果当前工作区明显不是目标项目，或没有可用于调研的业务代码但用户要求结合项目现状，先确认目标项目路径，不要直接写入当前目录。
   - 如果用户指定了目录，使用用户指定目录。
   - 如果用户未指定目录，根据功能名生成精确 slug，输出到 `./agent-works/{feature-slug}/requirement.md`。
   - 生成前检查目标 `requirement.md` 是否已存在；若已存在且用户没有明确要求更新该文件，不复用、不覆盖，改用更精细的 `{feature-slug}`。
   - 细化 `{feature-slug}` 时优先使用稳定业务语义，例如业务对象、流程、动作、页面、场景或配置范围；避免只用日期、随机数、`new`、`v2` 这类弱语义后缀。

2. 先判断是否已有上游“需求闭环分析”结果。
   - 按内容语义和用户明确指向识别上游输入，不绑定某个固定文件名；只有用户提供、明确引用或项目文档关系能够证明属于当前需求的材料，才作为继承依据，不能把同目录中的临时研究、历史草稿或其他功能文档自动视为上游结论。
   - 如果用户已经通过 `requirement-closure-designer` 或其他方式确认过“需求闭环分析”“完整功能闭环设计稿”“页面闭环草案”或同类上游结果，先把这些内容视为正式需求文档的主要输入，不要重新发散生成另一套需求闭环。
   - 对上游已确认内容，默认按“已确认结论”进入正式需求文档；只有当项目现状、代码实现、数据库结构、权限模型或用户新增约束与之冲突时，才指出冲突并重新确认。
   - 对上游输出中的“阻塞确认项”“非阻塞确认项”“推断决策”“必须实现 / 建议实现 / 暂不实现”范围划分和建议项处理状态，要优先继承和映射，不要在无冲突前提下改写含义。
   - 对上游已经明确的页面入口、页面职责、关键操作链路、状态流转、数据变化、权限边界和异常反馈，在正式需求文档中必须保留并展开，不得遗漏后重新生成一套页面承载关系。
   - 展开正式文档前，逐项核对上游功能点、页面入口、状态流转、权限边界、范围划分和确认结论在本文档中的承载位置；允许合并表达或补充项目事实，但不得静默删减、改变结论或把建议项升级为必须实现。确需调整时，明确写出冲突依据、影响和新的确认结论。
   - 如果用户只给了闭环分析结果但没有明确要求生成正式需求文档，先确认是否进入 `requirement.md` 生成，不要自动落文档。

3. 先调研项目现状，再写需求。
   - 阅读相关模块、页面、接口、服务、权限、配置和测试。
   - 如当前环境存在数据库 MCP 或可用数据库信息，结合表结构和实际数据分析需求。
   - 在文档中记录具体依据，例如文件路径、接口名、表名、字段名、枚举值、已有业务规则。
   - 如果无法访问数据库或关键上下文，要在文档中明确说明限制，不要编造事实。
   - 优先沿用当前项目已有业务规则、交互和技术承载模式；只有现有模式无法满足已确认需求时，才提出新的需求层抽象并说明原因。
   - 严格区分四类内容：可验证的项目事实、用户或上游已确认的业务结论、仍待确认的问题、基于依据作出的推断。代码现状只能证明“当前如何实现”，不能自动证明“业务必须如此”；推断也不能改写成项目事实或用户结论。
   - 调研深度以澄清需求边界、数据口径、外部契约和验收条件为限。表字段、接口字段、状态、事务或配置细节只有在它们属于既有事实、业务约束或会改变验收结果时才写入；类名、函数拆分、内部调用顺序等开发实现方案留给后续开发计划，不要把需求文档写成开发设计文档。

4. 采用专业角色视角补全需求。
   - 执行本技能时，默认以“资深业务领域专家 + 顶级系统业务需求分析师”的双重视角审视需求。
   - 业务领域不作为文档章节输出，只用于判断需求是否专业、业务闭环是否完整、术语是否准确、规则是否符合行业或项目语境、用户交互是否贴合真实使用场景。
   - 如果用户明确了领域，按用户给出的领域分析；如果未明确，结合初步需求、当前项目模块、已有业务流程、数据库表、页面交互和项目命名习惯自然推断。
   - 如果领域无法稳定推断，不要强行编造具体行业，继续以“当前项目业务专家 + 顶级系统业务需求分析师”的视角分析。
   - 只有当领域差异会明显改变需求边界、业务规则、专业术语、合规要求或用户体验，且项目现状无法判断时，才向用户确认具体领域。

5. 识别前端页面、交互和配置化需求。
   - 只要需求涉及新页面、更新页面、页面布局、组件、弹窗/抽屉、表格、图表、表单、设置项、偏好记忆、配置中心或通用前端能力，就必须先检查项目是否已有设计规范、组件库、页面模板、布局约定、样板页面或视觉验收方式；有则作为项目依据写入文档，没有则写明本次采用的最低交互约束。
   - 不要假设项目一定已有设计系统、布局系统或配置平台；先判断这是普通页面、复杂工作台、可配置页面，还是需要建设可复用基础能力。
   - 如果项目设计规范区分运行时复用组件、布局或配置组件种类、组件配置字段、页面注册能力等前端资产层级，需求文档只负责识别页面是否依赖已有通用能力、是否可能需要新增通用能力、是否涉及配置化承载；不要在需求阶段替开发计划替代性地指定具体代码实现。
   - “页面与交互开发需求”必须引用对应需求编号，并按页面和页面功能点组织；页面功能点必须覆盖本次需求范围内所有用户可触发、系统可反馈、状态会变化、数据会展示变化或提交变化的交互。
   - 每个页面条目必须说明页面结构、功能关系和变更范围；没有 HTML 原型且需要图示时，将页面布局图、功能关系图或变更区域图放到对应页面条目中，不要单独堆一段图示规则。
   - 如果需求包含新增页面，必须为每个新增页面生成对应 HTML 原型文件，并在页面条目中引用原型路径；原型文件放在需求文档输出目录的 `prototypes/` 子目录下，命名使用稳定页面语义，例如 `prototypes/{page-slug}.html`。
   - 生成或更新 HTML 原型时，必须遵守「页面原型生成规则」；原型中的正式业务控件、禁用/暂不实现控件、原型说明和原型演示控件要有清晰边界。
   - 如果只是更新既有页面，不要一律生成原型；当变更涉及新增主要区域、页面布局重排、跨组件联动、复杂表单/表格/图表、工作台、配置页、可视化状态或会明显影响开发理解的交互时，应生成更新后原型或变更区域原型。仅涉及文案、字段、小按钮、简单显隐、局部校验或低风险状态提示时，用页面布局说明、变更区域说明或图示即可，不强制生成 HTML 原型。
   - 如果需求包含“可配置”“自定义”“偏好”“模板”“预设”等能力，必须说明谁定义、谁配置、谁展示或渲染、谁保存及保存在哪里、谁读取、谁实际消费、谁校验，以及默认值、非法值处理、生效、失效和回退方式；不能只要求“保存配置”而不说明完整消费闭环。
   - 当需求明显会被多个页面、模块或场景复用时，必须把它识别为基础能力建设，补充样板场景、渐进接入范围和不做范围，避免把通用能力按单个页面功能草率定义。

6. 对不确定事项先分类，再决定是否确认。
   - 先区分“可通过项目代码、配置、数据库、现有文档或可用环境核实的客观事实”和“必须由用户决定的业务选择”。前者应先自行调研，不能因为尚未核实就转交用户拍板；只有无法访问必要依据，且该事实会阻塞需求闭环时，才说明限制并请求用户补充。
   - 阻塞确认：缺失答案会导致需求文档无法闭环，或会直接改变业务流程、数据结构、权限范围、状态流转、集成契约、开发方案、验收标准。
   - 非阻塞确认：缺失答案不影响开发计划启动，但会影响交互细节、默认值、提示文案、次要操作、展示顺序或实现复杂度。
   - 自行推断：项目已有同类模式、低风险、可回滚、不会改变数据结构或业务闭环的细节。
   - 对每个不确定项给出分类理由和影响，不要只罗列问题。
   - 阻塞确认必须先问用户。
   - 非阻塞确认先列给用户，并询问是否需要一并确认；如果用户选择确认，将这些问题升级为阻塞确认并逐项确认；如果用户选择不确认，则写入“遗留确认项”或按项目惯例形成“推断决策”。
   - 自行推断项直接写入文档并标注依据。
   - 如果待确认问题很多，先给出完整分类清单，再从最高影响的问题开始确认。不要设置固定数量上限，但每批问题要保持用户容易回答。
   - 如果上游闭环分析中已有同名确认项或推断决策，优先沿用原结论；只有在项目现状或用户新增信息导致结论失效时，才升级为新的确认问题。

7. 不要过早生成最终文档。
   - 阻塞确认问题没有解决前，不要直接写最终 `requirement.md`。
   - 如果用户明确要求先按假设推进，要在文档中标注“推断决策”。
   - 当需求涉及页面开发时，在写入最终 `requirement.md` 和必要原型文件前，必须先向用户展示待落地内容摘要，包括输出目录、页面清单、哪些页面需要 HTML 原型、哪些更新页面不需要原型及原因、关键布局假设、仍需确认的问题；只有用户明确确认后，才创建或覆盖 `requirement.md` 与必要的 `prototypes/*.html`。
   - 如果用户要求先产出草案，不要把草案当作最终落地文档；应先在对话中给出草案摘要或待确认清单，待确认后再写入文件。

8. 写入最终需求文档。
   - 必要时创建输出目录。
   - 如果存在需要 HTML 原型的页面，先创建或更新输出目录下的 `prototypes/` 子目录和对应原型文件，再在 `requirement.md` 页面条目中引用相对路径，例如 `prototypes/{page-slug}.html`。
   - 文档要足够具体，能直接指导后续开发计划和测试设计。
   - 为每个可追踪需求项分配稳定需求编号，便于后续开发计划建立需求与开发内容映射关系。
   - 更新既有需求文档时，保留仍有效需求项的原编号；调整章节顺序、合并表述或新增条目不得导致无关编号整体重排。只有需求含义发生实质变化时才新增编号，并说明原项如何处理。
   - 区分项目事实、用户确认结论、推断决策和风险。
   - “建议实现”和“业务亮点与优化建议”中的可执行条目必须带处理状态，便于后续判断是否已纳入、已实现或暂不处理。
   - 更新需求文档时，按最新确认结论和真实落地结果同步建议项状态，不保留已经失效的状态或只有建议文字、没有处理结论的条目。
   - 如果仍有未解决但不阻塞开发计划的问题，在文档末尾增加“遗留确认项”；如果关键问题未确认，不要生成最终文档，先继续确认。
   - 如果存在上游闭环分析结果，正式需求文档应以“继承并展开”作为默认策略，而不是“重新构造一套平行需求”。正式文档中的功能范围、详细功能需求、页面与交互开发需求、验收标准，应能追溯到上游闭环结果中的功能点、页面闭环草案、范围划分和确认结论。
   - 始终保留“建议开发顺序”章节；需求规模很小时写明建议单阶段完成，需求较大、跨多个模块或后续可能继续拆分时，给出基于本需求文档的模块拆分和推荐推进顺序。
   - 写入前做一次人工语义复核：按上游结论和本次用户要求逐项检查范围、编号、页面承载、状态、权限、确认结论和验收追溯；再检查文档内部是否存在同一概念多种口径、未确认推断被写成事实、建议项混入必须实现、验收标准找不到需求编号等问题。复核以语义判断为准，不能用绑定章节标题、文件名或具体业务字段的固定结构检查代替。

## 页面原型生成规则

- HTML 原型必须服务于需求澄清和后续开发理解，优先参考项目已有前端风格、DESIGN文档、组件库、布局密度、导航结构、表格/表单/弹窗模式和视觉规范；如果项目缺少可参考风格，采用克制、可实现、便于开发理解的低保真布局，不要把营销页、装饰性视觉或无关交互动效写入业务原型。
- 正式业务控件必须与需求定义的实际功能一致；出现按钮、菜单、筛选、提交、弹窗、抽屉、切换、联动、状态变化等交互元素时，必须提供对应的可用交互反馈，不能放置看似可用但无行为的功能控件。
- 无法实现或暂不纳入的交互必须禁用或显著标注为暂不实现，不得和正式可用操作使用同一视觉层级。
- 用于解释业务规则、字段含义、流程原因、验收口径或开发注意事项的内容，必须用区别于正式业务界面的颜色、边框、背景或标签样式标识为“原型说明/开发说明”，并避免伪装成真实页面文案、提示条、表单字段或业务组件。
- 仅用于评审演示状态或辅助理解的控件，必须放在“原型说明/开发说明”等批注区域内，按钮文案显式包含“原型演示”或同等标识；不得放入正式业务工具栏、筛选区、表单、表格操作列或页面主命令区。
- 如果原型提供隐藏批注能力，所有原型说明、开发说明和原型演示控件必须随批注一起隐藏，确保开发实现时能清晰区分线上功能和评审辅助内容。
- 原型辅助内容不能改变需求范围；真实线上功能仍以需求编号、页面功能点、验收标准和页面布局模板中的描述为准。
- 原型复核只检查它是否正确承载已定义的页面结构、操作链路、状态反馈、权限边界和关键数据口径；演示数据的数量、名称、排序结果等只有在用于证明业务规则或验收口径时才需要核对，不能把原型评审变成模拟数据实现审查。

## 需求编号规则

- 使用 `R1`、`R2` 这类本需求文档内稳定编号标识需求项；同一需求项在功能范围、详细功能需求、数据与接口需求、页面与交互开发需求和验收标准中必须使用同一编号。
- “必须实现”的需求项必须编号；“建议实现”和“暂不实现”也应编号，便于开发计划明确纳入、排除或后续处理。
- “建议实现”的条目必须带处理状态，状态只使用：`本次纳入`、`后续建议`、`已实现`、`待确认`、`不纳入`。`已实现` 必须有项目现状或后续实际落地依据。
- 一个编号只表达一个可验收的需求项；不要把多个无依赖关系的能力合并到同一个编号。
- 编号不得复用或在文档内改变含义；需求变更时新增编号或明确标注原编号的范围调整。
- 必须实现、建议实现和暂不实现必须互斥；同一需求项在不同章节重复引用时沿用同一编号和状态，不得换编号重复登记，也不得同时归入多个范围。
- 每个编号都要能追溯到用户诉求、项目依据、推断决策或确认结论；不能把未确认想法包装成已确认需求。

## 文档结构

除非用户指定其他格式，否则使用以下结构：

```markdown
# 需求文档

## 1. 背景与目标

说明为什么要做、解决什么问题、最终希望达到什么效果。

## 2. 当前项目现状

说明当前代码、业务流程、数据库、页面或接口的客观情况。涉及代码或表结构时，尽量给出文件路径、接口名、表名或字段依据。

## 3. 用户角色与使用场景

说明涉及哪些用户、角色、权限范围，以及典型操作场景。

## 4. 功能范围

按必须实现、建议实现、暂不实现划分范围，明确本次需求边界。

### 4.1 必须实现

| 需求编号 | 需求项 | 来源/依据 | 验收意图 |
| --- | --- | --- | --- |
| R1 |  |  |  |

### 4.2 建议实现

| 需求编号 | 需求项 | 来源/依据 | 纳入条件 | 处理状态 |
| --- | --- | --- | --- | --- |
| R2 |  |  |  | 后续建议 |

处理状态只使用：`本次纳入`、`后续建议`、`已实现`、`待确认`、`不纳入`。未纳入当前开发的建议项不能空置状态。

### 4.3 暂不实现

| 需求编号 | 需求项 | 不做原因 | 后续触发条件 |
| --- | --- | --- | --- |
| R3 |  |  |  |

暂不实现的内容必须说明不做什么、为什么本次不做、未来什么条件下可以再做，避免后续开发计划扩大范围。

## 5. 详细功能需求

每个功能点都要说明：
- 需求编号
- 功能描述
- 前置条件
- 操作流程
- 输入输出
- 校验规则
- 异常情况
- 权限与数据范围
- 对现有功能的影响

根据需求相关性覆盖新增、读取、修改、删除、查询筛选、导入导出、状态流转、权限边界、数据一致性及异常处理；不涉及的能力不必为凑模板而展开。

## 6. 数据与接口需求

说明涉及的数据表、字段、接口、数据流转和数据一致性要求。

只写会影响业务边界、上下游协作、兼容性或验收结果的契约，以及已经核实的项目事实；内部类设计、函数拆分、具体代码流程和不影响需求成立的实现选择交由开发计划确定。

## 7. 页面与交互开发需求

本章节用于说明页面、交互、弹窗/抽屉、配置入口或通用前端能力。本章节必须引用需求编号，覆盖本次范围内所有用户可触发、系统可反馈、状态会变化、数据会展示变化或提交变化的页面交互。

如果本需求确实不涉及任何页面或交互，明确写“不涉及页面开发”并说明原因。

### 7.1 页面 P1：页面名称

- 对应需求编号：
- 页面类型：新增页面 / 更新页面 / 弹窗 / 抽屉 / 配置页 / 工作台 / 其他
- 页面入口：
- 原型文件：新增页面必须引用 `prototypes/{page-slug}.html`；更新页面如生成 HTML 原型，引用对应路径；如无需 HTML 原型，写明“不生成原型”及原因。
- 页面布局：
  - 布局事实源：已生成或引用 HTML 原型时写“对齐 `prototypes/{page-slug}.html`”；无原型时说明采用文字说明、Mermaid、ASCII 草图或结构化线框图。
  - 页面区域：按页头、工具栏、主区、侧栏、表格、表单、详情区、分页、底部操作等区域分行说明，避免把完整布局堆成一段。
  - 主次关系：说明主工作区、辅助筛选区、详情承接区、配置入口之间的关系。
  - 核心模块与数据区：说明主要数据展示或编辑区域、字段/列、操作列、滚动归属、长内容处理。
  - 弹窗/抽屉/设置入口：说明触发入口、承载内容、提交/取消、关闭和刷新关系；没有则写“无”。
  - 关键状态与退化：说明加载、空态、错误、权限不足、保存中/失败、禁用态、窄视口退化等状态。
  - 图示要求：已引用 HTML 原型时不再另画 Mermaid、ASCII 草图或线框图；无 HTML 原型且复杂关系难以文字表达时才补图示。
- 数据来源：
- 权限与可见性：
- 组件复用与通用能力：
  - 已有通用能力：列出现有设计规范、组件库、布局模板、页面注册或配置能力中与本页面直接相关的能力；没有则写“未发现”。
  - 可能新增能力：仅说明需求层面的复用诉求、适用页面或场景、是否影响多个页面；具体实现、命名和拆分交给开发计划判断。
  - 配置化承载：如涉及布局、偏好、组件配置字段或页面注册能力，说明谁配置、谁看到、在哪里生效和如何回退；不涉及则写“无”。

#### 7.1.1 页面功能点：功能点名称

- 对应需求编号：
- 默认展示：
- 用户操作：
- 输入与校验：
- 状态变化：
- 保存/提交/取消：
- 加载状态：
- 空状态：
- 错误状态：
- 权限不足状态：
- 数据刷新：
- 与其他功能点的联动：
- 验收标准：

## 8. 边界条件与异常场景

列出需要考虑的边界情况和异常处理方式。

## 9. 性能、安全与兼容性要求

说明数据量、查询性能、并发、权限控制、敏感数据、安全校验和兼容性要求。

## 10. 业务亮点与优化建议

说明可以提升业务价值或用户体验的增强点。
只允许提出不改变核心业务闭环、不扩大必须实现范围的可选增强点；如果增强点会增加明显开发量、改变流程或引入新依赖，必须标注为后续建议。

| 编号/需求编号 | 优化建议 | 价值 | 处理状态 | 说明 |
| --- | --- | --- | --- | --- |
| R2 |  |  | 后续建议 |  |

处理状态沿用“建议实现”的状态枚举；可执行增强点如果会进入开发计划，应使用可追踪需求编号。

## 11. 验收标准

用可验证的方式描述完成标准，便于后续开发和测试。

验收标准必须引用需求编号，确保后续开发计划能判断每个需求项是否被覆盖。

“必须实现”和处理状态为“本次纳入”的需求项都必须至少有一条可观察、可执行的验收标准；“暂不实现”和未纳入建议项不得混入本次通过条件。

| 需求编号 | 验收标准 | 验证方式 |
| --- | --- | --- |
| R1 |  |  |

## 12. 建议开发顺序

用于说明推荐先做哪些模块、后做哪些模块，以及为什么这样排序。

如果需求规模很小、单次开发即可闭环，也保留本章节，并明确写“建议单阶段完成”及原因，避免后续再次判断是否需要拆分。

当需求较大或后续可能基于本文档继续拆出复杂模块的专属需求文档/开发计划时，本章节必须至少包含：
- 阶段或顺序编号
- 本阶段目标
- 本阶段建议覆盖的需求编号
- 依赖前置或前一阶段的原因
- 与其他模块或页面的关联关系
- 本阶段完成后的可验证里程碑
- 如需继续细化，下一步建议单独展开的复杂模块

本章节的目标是提供“基于需求的拆分和推进顺序”，不是替代正式开发计划；不要写成人天排期、成员分工或任务工时表。

## 13. 遗留确认项（仅在存在非阻塞未决问题时保留）

仅列出不影响当前开发计划启动、但后续仍建议确认的问题，并标注影响。没有这类问题时删除本章节。

```

## 质量要求

本节只做最终质量门禁；具体写法分别以前述执行流程、页面原型规则、需求编号规则和文档结构为准

- **事实可靠**：项目事实有可核实依据，未知信息、用户确认结论和推断决策边界清楚，不把当前实现或猜测包装成业务要求。
- **闭环完整**：按需求相关性覆盖主链路、状态、权限、数据、异常、页面反馈、性能安全与兼容性；配置能力同时覆盖定义、展示、保存、读取、消费、校验和回退。
- **全程可追溯**：上游结论能够追溯到稳定需求编号，编号贯穿范围、详细需求、数据接口、页面交互和可观察验收；建议项状态与真实结论一致。
- **可指导后续**：内容足以支持测试和开发计划，又不越界成具体实现设计；建议开发顺序体现需求依赖、模块和页面关系、基础能力前置条件及可验证里程碑。
