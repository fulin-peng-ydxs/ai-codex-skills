interface:
  display_name: "后端内存风险分析"
  short_description: "分析后端内存风险并输出完整报告到 /doc，附处理进度状态"
  default_prompt: "分析当前项目后端中可能存在的内存溢出、内存泄漏和高内存占用风险。生成当天报告前，先读取 doc/check/memory 下最新的一份已有报告作为基线，更新其中已有风险的处理进度、等级、证据和建议；如果发现新的风险，再新增这部分内容。最终将完整结构化报告输出到项目路径下的 doc/check/memory/{日期}.md。"
